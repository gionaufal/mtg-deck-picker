defmodule Mtgtopdecks do
  @moduledoc """
  Documentation for Mtgtopdecks.
  """

  # def compare_decks do
  #   #"Shock" |> (&Enum.member?(my_cards_map |> Map.keys, &1)).()
  #   # cards_map = Map.new(deck.cards, &Map.pop(&1, :card))
  #   decks = Scraper.extract_decks
  #   cards = Reader.read_cards
  #
  #   decks |> Enum.map(&compare_with_cards)
  # end
  #
  # defp compare_with_cards(deck) do
  #   deck.cards.map(&)
  # end
end
